# Assessment Reports

This directory is the only write target for the read-only MemoryCore assessment
phase.

Agents must write only their assigned report file here and must not mutate
implementation, docs, schemas, evals, fixtures, package files, CI, task state,
or existing packet files.
