# Review 2026-06-19

Status: scaffold.

Scope: current public-safe MemoryCore MVP scaffold.

## Verification Baseline

```bash
python3 -m memorycore.cli eval --public-safe
```

## Correctness

To review:

- request validation behavior
- backend routing determinism
- adapter normalization
- provenance and audit record shape
- skipped local-only eval reporting

## Readability

To review:

- module names and boundaries
- schema readability
- fixture clarity
- CLI command discoverability

## Architecture

To review:

- whether MemoryCore stays a control plane rather than becoming a memory store
- whether adapters remain backend-native
- whether CLI/MCP/OpenClaw caller paths preserve one contract

## Security

To review:

- content-sparse logs
- private/public boundary
- subprocess safety for future live QMD calls
- prompt-injection posture around retrieved content

## Performance

To review:

- public-safe eval runtime
- expected live backend timeout budgets
- ledger/audit write cost

## Open Items

- Complete once `docs/API_CONTRACT.md` and `docs/THREAT_MODEL.md` are filled in.

